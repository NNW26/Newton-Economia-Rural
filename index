<!DOCTYPE html>
<html lang="pt-BR" class="h-full">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SEAB PR — Cotações agrícolas DERAL (N.R. Laranjeiras do Sul)</title>
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Chart.js -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <!-- FontAwesome Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        sans: ['"Plus Jakarta Sans"', 'sans-serif'],
                    },
                    colors: {
                        brand: {
                            50: '#f0fdf4',
                            100: '#dcfce7',
                            500: '#22c55e',
                            600: '#16a34a',
                            700: '#15803d',
                            800: '#166534',
                            900: '#14532d',
                        }
                    }
                }
            }
        }
    </script>
    <style>
        body { font-family: 'Plus Jakarta Sans', sans-serif; }
        .custom-scrollbar::-webkit-scrollbar { width: 6px; height: 6px; }
        .custom-scrollbar::-webkit-scrollbar-track { background: #f1f5f9; }
        .custom-scrollbar::-webkit-scrollbar-thumb { background: #cbd5e1; border-radius: 4px; }
        .custom-scrollbar::-webkit-scrollbar-thumb:hover { background: #94a3b8; }
        
        .seab-highlight-badge {
            background: linear-gradient(135deg, #059669 0%, #10b981 100%);
            box-shadow: 0 4px 12px rgba(16, 185, 129, 0.2);
        }
    </style>
</head>
<body class="bg-slate-50 text-slate-800 min-h-full flex flex-col justify-between">

    <!-- TELA DE AUTENTICAÇÃO / PROTEÇÃO POR SENHA -->
    <div id="login-modal" class="fixed inset-0 z-[100] bg-slate-900/90 backdrop-blur-md flex items-center justify-center p-4">
        <div class="bg-white rounded-3xl p-8 max-w-md w-full shadow-2xl border border-slate-100 text-center space-y-6">
            <div class="w-16 h-16 bg-emerald-100 text-emerald-700 rounded-2xl flex items-center justify-center mx-auto text-2xl shadow-inner">
                <i class="fa-solid fa-lock"></i>
            </div>
            
            <div>
                <h2 class="text-xl font-extrabold text-slate-900">Acesso Restrito</h2>
                <p class="text-xs text-slate-500 mt-1">SEAB PR — Núcleo Regional de Laranjeiras do Sul</p>
                <p class="text-xs text-slate-400 mt-0.5">Insira a senha de acesso para visualizar o painel.</p>
            </div>

            <form onsubmit="checkPassword(event)" class="space-y-4">
                <div>
                    <input type="password" id="password-input" placeholder="Digite a senha..." required class="w-full px-4 py-3 text-sm text-center bg-slate-50 border border-slate-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-emerald-500 focus:bg-white transition-all font-semibold">
                    <p id="error-message" class="text-xs text-rose-600 font-semibold mt-2 hidden">
                        <i class="fa-solid fa-triangle-exclamation mr-1"></i> Senha incorreta. Tente novamente.
                    </p>
                </div>

                <button type="submit" class="w-full py-3 px-4 bg-emerald-600 hover:bg-emerald-700 text-white font-bold rounded-xl text-sm transition-all shadow-lg shadow-emerald-600/30">
                    Acessar Dashboard
                </button>
            </form>
            
            <p class="text-[11px] text-slate-400">Departamento de Economia Rural — DERAL (2026)</p>
        </div>
    </div>

    <!-- CONTEÚDO DO DASHBOARD (OCULTO ATÉ A AUTENTICAÇÃO) -->
    <div id="dashboard-content" class="hidden min-h-full flex flex-col flex-1">
        
        <!-- CABEÇALHO REORGANIZADO -->
        <header class="bg-white sticky top-0 z-50 border-b border-slate-200 shadow-sm">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 h-20 flex flex-col sm:flex-row items-center justify-between gap-3 py-2 sm:py-0">
                
                <!-- ESQUERDA: DESTAQUE PRINCIPAL PARA SEAB PR -->
                <div class="flex items-center space-x-3">
                    <div class="seab-highlight-badge text-white p-3 rounded-2xl flex items-center justify-center shadow-sm">
                        <i class="fa-solid fa-wheat-awn text-2xl"></i>
                    </div>
                    <div>
                        <div class="flex items-center gap-2">
                            <h1 class="font-extrabold text-xl text-slate-900 tracking-tight leading-tight">
                                SEAB PR
                            </h1>
                            <span class="bg-emerald-100 text-emerald-800 text-[11px] font-bold px-2 py-0.5 rounded-md border border-emerald-200">
                                Núcleo Regional de Laranjeiras do Sul
                            </span>
                        </div>
                        <p class="text-xs text-slate-500 font-medium">Painel de preços médios históricos (2019 – 2025)</p>
                    </div>
                </div>
                
                <!-- DIREITA: COTAÇÕES AGRÍCOLAS DERAL & CÂMBIO BACEN -->
                <div class="flex items-center gap-3">
                    <div class="bg-slate-100 border border-slate-200 text-slate-800 px-3.5 py-1.5 rounded-xl flex items-center gap-2">
                        <span class="w-2 h-2 rounded-full bg-emerald-500 animate-pulse"></span>
                        <span class="font-bold text-xs sm:text-sm tracking-tight">Cotações agrícolas DERAL</span>
                    </div>

                    <div id="exchange-rate-container" class="hidden sm:flex items-center gap-2 bg-slate-100 border border-slate-200 px-3 py-1.5 rounded-xl text-xs font-semibold text-slate-700">
                        <i class="fa-solid fa-dollar-sign text-emerald-600"></i>
                        <span>Dólar (USD):</span>
                        <span id="usd-rate-value" class="font-extrabold text-slate-900">Carregando...</span>
                        <button onclick="fetchUsdRate()" title="Atualizar câmbio do BACEN" class="text-slate-400 hover:text-emerald-600 transition-colors ml-1">
                            <i class="fa-solid fa-rotate-right text-[10px]"></i>
                        </button>
                    </div>
                </div>
            </div>
        </header>

        <main class="flex-1 max-w-7xl w-full mx-auto px-4 sm:px-6 lg:px-8 py-6 space-y-6">

            <!-- FILTROS & SELEÇÃO DE RECORTE TEMPORAL -->
            <section class="bg-white p-4 sm:p-5 rounded-2xl border border-slate-200 shadow-sm space-y-4">
                <div class="flex flex-col md:flex-row md:items-center justify-between gap-4 pb-3 border-b border-slate-100">
                    <div class="flex items-center space-x-2">
                        <i class="fa-solid fa-sliders text-emerald-600"></i>
                        <h2 class="font-bold text-slate-900 text-base">Filtros e recortes temporais</h2>
                    </div>
                    
                    <div class="flex flex-wrap items-center gap-3">
                        <div class="flex items-center gap-2 bg-slate-50 border border-slate-200 rounded-xl px-3 py-1.5">
                            <i class="fa-regular fa-calendar-days text-slate-500 text-xs"></i>
                            <label for="year-select" class="text-xs font-bold text-slate-600">Ano:</label>
                            <select id="year-select" onchange="changeYear(this.value)" class="bg-transparent text-xs font-bold text-slate-900 focus:outline-none cursor-pointer">
                                <option value="all">Todos os anos (Visão histórica)</option>
                                <option value="2019">2019</option>
                                <option value="2020">2020</option>
                                <option value="2021">2021</option>
                                <option value="2022">2022</option>
                                <option value="2023">2023</option>
                                <option value="2024">2024</option>
                                <option value="2025" selected>2025</option>
                            </select>
                        </div>

                        <div class="flex flex-wrap gap-1 bg-slate-100 p-1 rounded-xl" id="category-tabs">
                            <button onclick="selectCategory('all')" id="cat-all" class="cat-btn px-3 py-1.5 text-xs font-bold rounded-lg transition-all bg-white text-emerald-700 shadow-sm">Todos</button>
                            <button onclick="selectCategory('graos')" id="cat-graos" class="cat-btn px-3 py-1.5 text-xs font-semibold rounded-lg text-slate-600 hover:text-slate-900 transition-all">🌾 Grãos</button>
                            <button onclick="selectCategory('pecuaria')" id="cat-pecuaria" class="cat-btn px-3 py-1.5 text-xs font-semibold rounded-lg text-slate-600 hover:text-slate-900 transition-all">🐄 Pecuária</button>
                        </div>
                    </div>
                </div>

                <div>
                    <p class="text-xs font-semibold text-slate-400 mb-2 uppercase tracking-wider">Commodities monitoradas:</p>
                    <div class="flex flex-wrap gap-2" id="product-buttons"></div>
                </div>
            </section>

            <!-- CARDS DE RESUMO KPI -->
            <section class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4" id="kpi-cards"></section>

            <!-- GRÁFICOS -->
            <section class="grid grid-cols-1 lg:grid-cols-3 gap-6">
                <div class="lg:col-span-2 bg-white p-5 rounded-2xl border border-slate-200 shadow-sm flex flex-col justify-between">
                    <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-2 mb-4">
                        <div>
                            <h3 class="font-bold text-slate-900 text-base flex items-center gap-2">
                                <i class="fa-solid fa-chart-line text-emerald-600"></i>
                                Evolução temporal dos preços médios (R$)
                            </h3>
                            <p class="text-xs text-slate-500" id="chart-subtitle">Histórico comparativo mensal apurado em Laranjeiras do Sul</p>
                        </div>
                        <div id="chart-period-badge" class="text-xs text-emerald-800 bg-emerald-50 font-bold px-3 py-1 rounded-full border border-emerald-200 self-start sm:self-auto">
                            Ano: 2025
                        </div>
                    </div>
                    <div class="relative w-full h-[330px]">
                        <canvas id="lineChart"></canvas>
                    </div>
                </div>

                <div class="bg-white p-5 rounded-2xl border border-slate-200 shadow-sm flex flex-col justify-between">
                    <div class="flex items-center justify-between mb-4">
                        <div>
                            <h3 class="font-bold text-slate-900 text-base flex items-center gap-2">
                                <i class="fa-solid fa-chart-column text-indigo-600"></i>
                                Média de preços por produto
                            </h3>
                            <p class="text-xs text-slate-500" id="bar-chart-subtitle">Valores médios apurados no período selecionado</p>
                        </div>
                    </div>
                    <div class="relative w-full h-[330px]">
                        <canvas id="barChart"></canvas>
                    </div>
                </div>
            </section>

            <!-- TABELA DE DADOS -->
            <section class="bg-white rounded-2xl border border-slate-200 shadow-sm overflow-hidden">
                <div class="p-5 border-b border-slate-100 flex flex-col sm:flex-row sm:items-center justify-between gap-4">
                    <div>
                        <h3 class="font-bold text-slate-900 text-base flex items-center gap-2">
                            <i class="fa-solid fa-table-list text-slate-700"></i>
                            Consolidado histórico de cotações (R$)
                        </h3>
                        <p class="text-xs text-slate-500">Apuração mensal em Reais — SEAB PR Laranjeiras do Sul</p>
                    </div>
                    <div class="relative w-full sm:w-64">
                        <i class="fa-solid fa-magnifying-glass absolute left-3 top-1/2 -translate-y-1/2 text-slate-400 text-xs"></i>
                        <input type="text" id="table-search" oninput="filterTable()" placeholder="Buscar produto ou unidade..." class="w-full pl-9 pr-3 py-2 text-xs bg-slate-50 border border-slate-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-emerald-500 focus:bg-white transition-all">
                    </div>
                </div>

                <div class="overflow-x-auto custom-scrollbar">
                    <table class="w-full text-left border-collapse text-xs">
                        <thead>
                            <tr class="bg-slate-900 text-slate-200 uppercase tracking-wider text-[11px] font-semibold">
                                <th class="py-3.5 px-4 min-w-[140px]">Produto</th>
                                <th class="py-3.5 px-4">Unid.</th>
                                <th class="py-3.5 px-4 text-center">Ano</th>
                                <th class="py-3.5 px-3 text-right">Jan</th>
                                <th class="py-3.5 px-3 text-right">Fev</th>
                                <th class="py-3.5 px-3 text-right">Mar</th>
                                <th class="py-3.5 px-3 text-right">Abr</th>
                                <th class="py-3.5 px-3 text-right">Mai</th>
                                <th class="py-3.5 px-3 text-right">Jun</th>
                                <th class="py-3.5 px-3 text-right">Jul</th>
                                <th class="py-3.5 px-3 text-right">Ago</th>
                                <th class="py-3.5 px-3 text-right">Set</th>
                                <th class="py-3.5 px-3 text-right">Out</th>
                                <th class="py-3.5 px-3 text-right">Nov</th>
                                <th class="py-3.5 px-3 text-right">Dez</th>
                                <th class="py-3.5 px-4 text-right bg-slate-800">Média</th>
                            </tr>
                        </thead>
                        <tbody id="table-body" class="divide-y divide-slate-100"></tbody>
                    </table>
                </div>
            </section>

        </main>

        <!-- RODAPÉ INSTITUCIONAL -->
        <footer class="bg-white border-t border-slate-200 text-slate-600 text-xs py-6 mt-10">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 text-center sm:text-left">
                <p class="font-medium text-slate-700">
                    Secretaria de Estado da Agricultura e do Abastecimento do Paraná.
                </p>
                <p class="text-slate-500 mt-0.5">
                    Departamento de Economia Rural. Laranjeiras do Sul, 2026, versão 1.1
                </p>
            </div>
        </footer>
    </div>

    <script>
        // CONFIGURAÇÃO DA SENHA DE ACESSO
        const CORRECT_PASSWORD = "seab2026"; // <-- Altere a senha aqui se desejar

        function checkPassword(e) {
            e.preventDefault();
            const inputVal = document.getElementById('password-input').value;
            const errorMsg = document.getElementById('error-message');

            if (inputVal === CORRECT_PASSWORD) {
                document.getElementById('login-modal').classList.add('hidden');
                document.getElementById('dashboard-content').classList.remove('hidden');
                
                // Inicializa os dados e gráficos apenas após autenticação
                fetchUsdRate();
                renderProductButtons();
                updateDashboard();
                renderTable();
            } else {
                errorMsg.classList.remove('hidden');
            }
        }

        // DATASET COMPLETO HISTÓRICO (2019-2025)
        const fullDataset = [
            {
                id: 'soja',
                name: 'Soja',
                category: 'graos',
                unit: 'Saca 60kg',
                icon: 'fa-seedling',
                color: '#16a34a',
                history: {
                    2019: [68.50, 69.20, 68.80, 67.50, 68.10, 71.00, 72.30, 74.50, 76.80, 78.20, 81.00, 82.50],
                    2020: [80.50, 82.00, 89.10, 93.40, 92.80, 96.50, 104.20, 115.00, 128.40, 145.00, 152.00, 148.50],
                    2021: [155.00, 158.50, 162.00, 165.20, 171.00, 168.40, 162.50, 164.00, 167.50, 166.00, 163.20, 168.00],
                    2022: [172.50, 186.00, 192.50, 185.00, 182.40, 180.00, 178.50, 177.00, 175.20, 174.00, 172.00, 170.50],
                    2023: [168.00, 162.50, 150.00, 132.00, 124.50, 126.00, 128.50, 131.00, 134.20, 132.00, 135.50, 138.00],
                    2024: [122.00, 112.50, 114.00, 116.50, 118.00, 120.50, 122.00, 125.40, 124.00, 126.50, 128.00, 127.50],
                    2025: [124.50, 121.00, 118.50, 116.00, 113.05, 113.58, 120.07, 128.81, 130.50, 132.00, 131.20, 133.00]
                }
            },
            {
                id: 'milho',
                name: 'Milho',
                category: 'graos',
                unit: 'Saca 60kg',
                icon: 'fa-wheat-wheat',
                color: '#eab308',
                history: {
                    2019: [29.50, 30.20, 28.50, 26.80, 27.20, 28.00, 28.80, 29.50, 31.00, 34.50, 38.00, 40.20],
                    2020: [41.00, 43.50, 48.00, 45.20, 42.00, 41.50, 44.00, 50.50, 56.80, 68.00, 72.50, 70.00],
                    2021: [72.00, 75.50, 81.00, 88.50, 92.00, 89.00, 87.50, 91.20, 88.00, 84.50, 82.00, 85.50],
                    2022: [88.00, 91.50, 94.00, 88.20, 85.00, 83.50, 81.00, 82.50, 80.00, 78.50, 79.00, 81.20],
                    2023: [80.00, 78.50, 72.00, 58.00, 51.50, 48.00, 47.50, 49.00, 52.00, 53.50, 55.00, 58.20],
                    2024: [56.00, 52.50, 51.00, 53.00, 54.50, 53.80, 52.00, 54.00, 56.50, 58.00, 61.20, 63.50],
                    2025: [62.00, 59.50, 58.00, 57.20, 56.75, 56.70, 56.17, 60.24, 61.50, 62.80, 64.00, 65.50]
                }
            },
            {
                id: 'trigo',
                name: 'Trigo',
                category: 'graos',
                unit: 'Saca 60kg',
                icon: 'fa-bread-slice',
                color: '#f97316',
                history: {
                    2019: [42.00, 43.50, 44.00, 45.20, 46.00, 47.50, 48.00, 47.00, 45.50, 46.20, 48.00, 49.50],
                    2020: [50.00, 52.00, 56.50, 61.00, 63.20, 65.00, 66.80, 68.50, 72.00, 75.00, 73.50, 71.00],
                    2021: [72.50, 74.00, 76.00, 78.20, 81.50, 84.00, 86.50, 88.00, 85.00, 82.00, 83.50, 86.00],
                    2022: [90.00, 96.00, 105.00, 108.00, 112.50, 115.00, 113.00, 106.00, 98.00, 92.50, 91.00, 92.80],
                    2023: [91.50, 88.00, 83.00, 76.50, 72.00, 68.50, 65.00, 62.50, 58.00, 56.50, 57.20, 60.00],
                    2024: [58.50, 56.00, 55.20, 57.00, 62.50, 65.00, 66.80, 64.50, 62.00, 63.50, 65.00, 66.20],
                    2025: [65.00, 64.20, 65.50, 66.80, 67.45, 70.45, 71.43, 75.02, 76.50, 77.20, 78.00, 79.50]
                }
            },
            {
                id: 'feijao',
                name: 'Feijão Preto',
                category: 'graos',
                unit: 'Saca 60kg',
                icon: 'fa-bowl-rice',
                color: '#6366f1',
                history: {
                    2019: [110.00, 135.00, 160.00, 145.00, 130.00, 125.00, 120.00, 128.00, 135.00, 142.00, 150.00, 165.00],
                    2020: [175.00, 190.00, 220.00, 260.00, 280.00, 250.00, 230.00, 245.00, 260.00, 275.00, 285.00, 290.00],
                    2021: [280.00, 265.00, 250.00, 240.00, 235.00, 220.00, 215.00, 225.00, 240.00, 255.00, 260.00, 270.00],
                    2022: [265.00, 250.00, 245.00, 260.00, 275.00, 280.00, 270.00, 260.00, 255.00, 265.00, 270.00, 285.00],
                    2023: [290.00, 280.00, 260.00, 240.00, 225.00, 210.00, 200.00, 215.00, 230.00, 245.00, 260.00, 275.00],
                    2024: [280.00, 260.00, 240.00, 220.00, 210.00, 205.00, 198.00, 205.00, 215.00, 220.00, 210.00, 205.00],
                    2025: [215.00, 210.00, 208.00, 205.00, 203.00, 193.50, 184.78, 189.52, 192.00, 195.00, 198.00, 202.00]
                }
            },
            {
                id: 'feijao_cores',
                name: 'Feijão Cores',
                category: 'graos',
                unit: 'Saca 60kg',
                icon: 'fa-cubes-stacked',
                color: '#8b5cf6',
                history: {
                    2019: [130.00, 165.00, 190.00, 170.00, 150.00, 140.00, 135.00, 145.00, 155.00, 160.00, 175.00, 190.00],
                    2020: [200.00, 220.00, 260.00, 310.00, 330.00, 290.00, 270.00, 280.00, 295.00, 310.00, 320.00, 315.00],
                    2021: [300.00, 285.00, 270.00, 260.00, 250.00, 240.00, 230.00, 245.00, 260.00, 275.00, 280.00, 290.00],
                    2022: [280.00, 270.00, 285.00, 310.00, 340.00, 350.00, 330.00, 310.00, 300.00, 315.00, 330.00, 345.00],
                    2023: [360.00, 340.00, 310.00, 270.00, 240.00, 220.00, 210.00, 225.00, 240.00, 255.00, 270.00, 285.00],
                    2024: [290.00, 270.00, 250.00, 230.00, 220.00, 210.00, 205.00, 215.00, 225.00, 230.00, 225.00, 220.00],
                    2025: [225.00, 220.00, 215.00, 210.00, 208.50, 202.00, 195.50, 198.80, 204.00, 208.00, 212.00, 215.00]
                }
            },
            {
                id: 'boi',
                name: 'Boi em Pé',
                category: 'pecuaria',
                unit: 'Arroba (@)',
                icon: 'fa-cow',
                color: '#dc2626',
                history: {
                    2019: [150.00, 152.00, 153.50, 155.00, 154.00, 152.50, 155.00, 158.00, 162.00, 175.00, 205.00, 210.00],
                    2020: [195.00, 198.00, 202.00, 200.00, 205.00, 215.00, 225.00, 240.00, 255.00, 268.00, 280.00, 272.00],
                    2021: [285.00, 295.00, 310.00, 315.00, 312.00, 318.00, 320.00, 315.00, 305.00, 290.00, 300.00, 318.00],
                    2022: [335.00, 340.00, 345.00, 332.00, 320.00, 310.00, 305.00, 300.00, 292.00, 285.00, 280.00, 288.00],
                    2023: [290.00, 285.00, 278.00, 265.00, 250.00, 242.00, 235.00, 220.00, 210.00, 225.00, 232.00, 245.00],
                    2024: [240.00, 235.00, 230.00, 225.00, 220.00, 218.00, 222.00, 230.00, 245.00, 265.00, 285.00, 310.00],
                    2025: [320.00, 330.00, 338.00, 342.00, 345.75, 343.25, 335.87, 354.05, 358.00, 362.00, 360.00, 365.00]
                }
            },
            {
                id: 'vaca',
                name: 'Vaca em Pé',
                category: 'pecuaria',
                unit: 'Arroba (@)',
                icon: 'fa-cow',
                color: '#a855f7',
                history: {
                    2019: [135.00, 137.00, 138.00, 140.00, 139.00, 137.50, 140.00, 142.50, 147.00, 158.00, 185.00, 190.00],
                    2020: [178.00, 180.00, 185.00, 182.00, 188.00, 196.00, 205.00, 218.00, 232.00, 245.00, 255.00, 248.00],
                    2021: [260.00, 270.00, 282.00, 288.00, 285.00, 290.00, 292.00, 288.00, 278.00, 265.00, 272.00, 288.00],
                    2022: [300.00, 305.00, 310.00, 298.00, 288.00, 278.00, 272.00, 268.00, 260.00, 252.00, 248.00, 255.00],
                    2023: [258.00, 252.00, 245.00, 232.00, 220.00, 212.00, 205.00, 192.00, 185.00, 196.00, 202.00, 212.00],
                    2024: [208.00, 202.00, 198.00, 192.00, 188.00, 186.00, 190.00, 198.00, 210.00, 228.00, 248.00, 270.00],
                    2025: [280.00, 288.00, 295.00, 298.00, 300.00, 300.00, 292.39, 305.71, 310.00, 314.00, 312.00, 318.00]
                }
            },
            {
                id: 'suino',
                name: 'Suíno em Pé',
                category: 'pecuaria',
                unit: 'Kg',
                icon: 'fa-piggy-bank',
                color: '#ec4899',
                history: {
                    2019: [3.20, 3.10, 3.40, 3.80, 3.75, 3.60, 3.85, 4.10, 4.30, 4.50, 5.20, 5.40],
                    2020: [4.80, 4.60, 4.40, 4.10, 4.30, 4.80, 5.60, 6.80, 8.20, 9.50, 9.80, 8.90],
                    2021: [7.80, 7.20, 6.80, 6.20, 6.50, 6.10, 5.90, 6.40, 6.80, 6.20, 5.80, 6.10],
                    2022: [5.60, 5.20, 5.80, 6.20, 6.50, 6.80, 6.60, 6.40, 6.20, 6.50, 6.80, 7.10],
                    2023: [6.80, 6.40, 6.20, 5.80, 5.40, 5.10, 5.30, 5.60, 5.90, 6.10, 6.30, 6.50],
                    2024: [6.20, 5.90, 5.70, 5.80, 6.10, 6.40, 6.60, 6.80, 7.20, 7.50, 7.80, 7.60],
                    2025: [6.80, 6.10, 5.60, 5.30, 5.12, 4.84, 4.74, 4.64, 4.80, 4.95, 5.10, 5.25]
                }
            }
        ];

        const monthNames = ['Jan', 'Fev', 'Mar', 'Abr', 'Mai', 'Jun', 'Jul', 'Ago', 'Set', 'Out', 'Nov', 'Dez'];

        let selectedYear = '2025';
        let activeCategory = 'all';
        let selectedProduct = 'all';
        let lineChartInstance = null;
        let barChartInstance = null;

        async function fetchUsdRate() {
            const usdValEl = document.getElementById('usd-rate-value');
            if (usdValEl) usdValEl.innerText = 'Carregando...';
            try {
                const response = await fetch('https://economia.awesomeapi.com.br/json/last/USD-BRL');
                if (!response.ok) throw new Error('Falha na API');
                const data = await response.json();
                const rate = parseFloat(data.USDBRL.bid);
                if (usdValEl) usdValEl.innerText = `R$ ${rate.toFixed(2)}`;
            } catch (err) {
                if (usdValEl) usdValEl.innerText = 'R$ 5,42*';
            }
        }

        function getProductMetrics(item, year) {
            let prices = [];
            if (year === 'all') {
                Object.keys(item.history).sort().forEach(y => {
                    prices = prices.concat(item.history[y]);
                });
            } else {
                prices = item.history[year] || [];
            }

            const validPrices = prices.filter(p => p !== null && p !== undefined);
            if (validPrices.length === 0) {
                return { latest: 0, prev: 0, avg: 0, accumVarPct: 0, prices: [] };
            }

            const latest = validPrices[validPrices.length - 1];
            const first = validPrices[0];
            const sum = validPrices.reduce((a, b) => a + b, 0);
            const avg = sum / validPrices.length;
            const accumVarPct = first !== 0 ? ((latest - first) / first) * 100 : 0;

            return { latest, first, avg, accumVarPct, prices: validPrices };
        }

        function changeYear(year) {
            selectedYear = year;
            document.getElementById('chart-period-badge').innerText = year === 'all' ? 'Período: 2019 – 2025' : `Ano: ${year}`;
            updateDashboard();
            renderTable();
        }

        function selectCategory(cat) {
            activeCategory = cat;
            selectedProduct = 'all';

            document.querySelectorAll('.cat-btn').forEach(btn => {
                btn.className = 'cat-btn px-3 py-1.5 text-xs font-semibold rounded-lg text-slate-600 hover:text-slate-900 transition-all';
            });
            const activeTab = document.getElementById(`cat-${cat}`);
            if (activeTab) {
                activeTab.className = 'cat-btn px-3 py-1.5 text-xs font-bold rounded-lg transition-all bg-white text-emerald-700 shadow-sm';
            }

            renderProductButtons();
            updateDashboard();
            renderTable();
        }

        function renderProductButtons() {
            const container = document.getElementById('product-buttons');
            let filtered = fullDataset;
            if (activeCategory !== 'all') {
                filtered = fullDataset.filter(d => d.category === activeCategory);
            }

            let html = `
                <button onclick="selectProduct('all')" class="prod-btn px-3 py-1.5 text-xs font-medium rounded-xl border transition-all ${selectedProduct === 'all' ? 'bg-slate-900 text-white border-slate-900 font-semibold shadow-sm' : 'bg-slate-50 text-slate-700 border-slate-200 hover:bg-slate-100'}">
                    Todos os produtos (${filtered.length})
                </button>
            `;

            filtered.forEach(item => {
                const isSelected = selectedProduct === item.id;
                html += `
                    <button onclick="selectProduct('${item.id}')" class="prod-btn px-3 py-1.5 text-xs font-medium rounded-xl border flex items-center gap-1.5 transition-all ${isSelected ? 'bg-emerald-600 text-white border-emerald-600 font-semibold shadow-sm' : 'bg-slate-50 text-slate-700 border-slate-200 hover:bg-slate-100'}">
                        <span class="w-2 h-2 rounded-full" style="background-color: ${item.color}"></span>
                        ${item.name}
                    </button>
                `;
            });

            container.innerHTML = html;
        }

        function selectProduct(id) {
            selectedProduct = id;
            renderProductButtons();
            updateDashboard();
        }

        function updateDashboard() {
            renderKPIs();
            renderLineChart();
            renderBarChart();
        }

        function renderKPIs() {
            const container = document.getElementById('kpi-cards');
            let itemsToDisplay = fullDataset;

            if (selectedProduct !== 'all') {
                itemsToDisplay = fullDataset.filter(d => d.id === selectedProduct);
            } else if (activeCategory !== 'all') {
                itemsToDisplay = fullDataset.filter(d => d.category === activeCategory);
            }

            const displayItems = itemsToDisplay.slice(0, 4);

            let html = '';
            displayItems.forEach(item => {
                const m = getProductMetrics(item, selectedYear);
                const isUp = m.accumVarPct >= 0;
                const badgeColor = isUp ? 'bg-emerald-50 text-emerald-700 border-emerald-200' : 'bg-rose-50 text-rose-700 border-rose-200';
                const arrowIcon = isUp ? 'fa-arrow-trend-up' : 'fa-arrow-trend-down';

                html += `
                    <div class="bg-white p-4 rounded-2xl border border-slate-200 shadow-sm flex flex-col justify-between hover:border-emerald-300 transition-all">
                        <div class="flex items-center justify-between">
                            <div class="flex items-center space-x-2">
                                <div class="w-8 h-8 rounded-lg flex items-center justify-center text-white text-xs shadow-sm" style="background-color: ${item.color}">
                                    <i class="fa-solid ${item.icon}"></i>
                                </div>
                                <div>
                                    <h4 class="font-bold text-slate-900 text-xs">${item.name}</h4>
                                    <p class="text-[10px] text-slate-400">${item.unit}</p>
                                </div>
                            </div>
                            <span class="text-[10px] font-bold px-1.5 py-0.5 rounded border ${badgeColor} flex items-center gap-0.5">
                                <i class="fa-solid ${arrowIcon}"></i>
                                ${m.accumVarPct > 0 ? '+' : ''}${m.accumVarPct.toFixed(1)}%
                            </span>
                        </div>

                        <div class="mt-3 pt-2.5 border-t border-slate-100 flex items-end justify-between">
                            <div>
                                <span class="text-[9px] text-slate-400 font-bold uppercase tracking-wider">Última cotação</span>
                                <div class="text-base font-extrabold text-slate-900">R$ ${m.latest.toLocaleString('pt-BR', {minimumFractionDigits: 2, maximumFractionDigits: 2})}</div>
                            </div>
                            <div class="text-right">
                                <span class="text-[9px] text-slate-400 font-bold uppercase tracking-wider">Média no período</span>
                                <div class="text-xs font-semibold text-slate-600">R$ ${m.avg.toFixed(2)}</div>
                            </div>
                        </div>
                    </div>
                `;
            });

            container.innerHTML = html;
        }

        function renderLineChart() {
            const ctx = document.getElementById('lineChart').getContext('2d');
            
            let filtered = fullDataset;
            if (selectedProduct !== 'all') {
                filtered = fullDataset.filter(d => d.id === selectedProduct);
            } else if (activeCategory !== 'all') {
                filtered = fullDataset.filter(d => d.category === activeCategory);
            }

            let labels = [];
            if (selectedYear === 'all') {
                [2019, 2020, 2021, 2022, 2023, 2024, 2025].forEach(yr => {
                    monthNames.forEach(m => {
                        labels.push(`${m}/${yr.toString().substr(2)}`);
                    });
                });
            } else {
                labels = monthNames;
            }

            const hasSuino = filtered.some(d => d.id === 'suino');
            const hasOthers = filtered.some(d => d.id !== 'suino');
            const useDualAxis = hasSuino && hasOthers;

            const chartDatasets = filtered.map(item => {
                const metrics = getProductMetrics(item, selectedYear);
                const isSuino = item.id === 'suino';

                return {
                    label: `${item.name} (${item.unit})`,
                    data: metrics.prices,
                    borderColor: item.color,
                    backgroundColor: item.color + '15',
                    borderWidth: isSuino ? 3 : 2,
                    borderDash: isSuino && useDualAxis ? [4, 4] : [],
                    yAxisID: (isSuino && useDualAxis) ? 'y2' : 'y',
                    tension: 0.25,
                    pointRadius: selectedYear === 'all' ? 1.5 : 3.5,
                    fill: filtered.length === 1
                };
            });

            if (lineChartInstance) lineChartInstance.destroy();

            const scalesConfig = {
                x: {
                    grid: { display: false },
                    ticks: { font: { family: 'Plus Jakarta Sans', size: 10 } }
                },
                y: {
                    type: 'linear',
                    display: true,
                    position: 'left',
                    title: { display: true, text: 'Preço (R$ / Saca ou @)', font: { size: 10, weight: 'bold' } },
                    grid: { color: '#f1f5f9' },
                    ticks: { callback: value => 'R$ ' + value }
                }
            };

            if (useDualAxis) {
                scalesConfig.y2 = {
                    type: 'linear',
                    display: true,
                    position: 'right',
                    title: { display: true, text: 'Suíno em Pé (R$ / Kg)', font: { size: 10, weight: 'bold' }, color: '#ec4899' },
                    grid: { drawOnChartArea: false },
                    ticks: { color: '#ec4899', callback: value => 'R$ ' + value.toFixed(2) }
                };
            }

            lineChartInstance = new Chart(ctx, {
                type: 'line',
                data: { labels, datasets: chartDatasets },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: { position: 'bottom', labels: { boxWidth: 10, font: { family: 'Plus Jakarta Sans', size: 10, weight: '600' } } },
                        tooltip: {
                            backgroundColor: '#0f172a',
                            callbacks: {
                                label: context => ` ${context.dataset.label}: R$ ${context.parsed.y.toFixed(2)}`
                            }
                        }
                    },
                    scales: scalesConfig
                }
            });
        }

        function renderBarChart() {
            const ctx = document.getElementById('barChart').getContext('2d');
            
            let filtered = fullDataset;
            if (selectedProduct !== 'all') {
                filtered = fullDataset.filter(d => d.id === selectedProduct);
            } else if (activeCategory !== 'all') {
                filtered = fullDataset.filter(d => d.category === activeCategory);
            }

            const labels = filtered.map(d => d.name);
            const dataValues = filtered.map(d => getProductMetrics(d, selectedYear).avg);
            const barColors = filtered.map(d => d.color);

            if (barChartInstance) barChartInstance.destroy();

            barChartInstance = new Chart(ctx, {
                type: 'bar',
                data: {
                    labels: labels,
                    datasets: [{
                        label: 'Preço médio (R$)',
                        data: dataValues,
                        backgroundColor: barColors,
                        borderRadius: 6
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: { display: false },
                        tooltip: {
                            backgroundColor: '#0f172a',
                            callbacks: {
                                label: context => ` Média no período: R$ ${context.parsed.y.toFixed(2)}`
                            }
                        }
                    },
                    scales: {
                        x: { grid: { display: false }, ticks: { font: { family: 'Plus Jakarta Sans', size: 10, weight: '600' } } },
                        y: { grid: { color: '#f1f5f9' }, ticks: { callback: value => 'R$ ' + value } }
                    }
                }
            });
        }

        function renderTable() {
            const tbody = document.getElementById('table-body');
            const searchVal = document.getElementById('table-search').value.toLowerCase();

            let filtered = fullDataset.filter(item => {
                const matchCategory = activeCategory === 'all' || item.category === activeCategory;
                const matchProduct = selectedProduct === 'all' || item.id === selectedProduct;
                const matchSearch = item.name.toLowerCase().includes(searchVal) || item.unit.toLowerCase().includes(searchVal);
                return matchCategory && matchProduct && matchSearch;
            });

            if (filtered.length === 0) {
                tbody.innerHTML = `<tr><td colspan="16" class="text-center py-6 text-slate-400 font-medium">Nenhum produto ou cotação encontrada.</td></tr>`;
                return;
            }

            let html = '';
            const yearsToDisplay = selectedYear === 'all' ? [2019, 2020, 2021, 2022, 2023, 2024, 2025] : [parseInt(selectedYear)];

            filtered.forEach(item => {
                yearsToDisplay.forEach(yr => {
                    const monthPrices = item.history[yr] || Array(12).fill(0);
                    const sum = monthPrices.reduce((a, b) => a + b, 0);
                    const avg = sum / monthPrices.length;

                    html += `
                        <tr class="hover:bg-slate-50 transition-all font-medium">
                            <td class="py-3 px-4 font-bold text-slate-900 flex items-center gap-2">
                                <span class="w-2.5 h-2.5 rounded-full flex-shrink-0" style="background-color: ${item.color}"></span>
                                ${item.name}
                            </td>
                            <td class="py-3 px-4 text-slate-500">${item.unit}</td>
                            <td class="py-3 px-4 text-center font-bold text-emerald-800 bg-emerald-50/50">${yr}</td>
                            ${monthPrices.map(p => `<td class="py-3 px-3 text-right text-slate-700">${p ? p.toFixed(2) : '-'}</td>`).join('')}
                            <td class="py-3 px-4 text-right font-extrabold text-slate-900 bg-slate-100">R$ ${avg.toFixed(2)}</td>
                        </tr>
                    `;
                });
            });

            tbody.innerHTML = html;
        }

        function filterTable() {
            renderTable();
        }
    </script>
</body>
</html>
